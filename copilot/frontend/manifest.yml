name: frontend
type: Load Balanced Web Service

image:
  build: ./frontend/frontend/bus-tracker-app/Dockerfile
  port: 80

http:
  path: '/'
  healthcheck: '/'

cpu: 256
memory: 512
count: 1
exec: true
